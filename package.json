{
  "name": "@louy/ts-gettext-extractor",
  "version": "0.0.3",
  "main": "index.js",
  "repository": "https://github.com/louy/ts-gettext-extractor.git",
  "author": "Louay <louy@users.noreply.github.com>",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.2.2",
  "scripts": {
    "version": "yarn workspaces foreach --all version $npm_package_version && git add packages/*/package.json && yarn bump:cargo",
    "prebump": "cargo check",
    "bump": "npm version --force -m 'v%s' --git-tag-version=true",
    "bump:cargo": "perl -i -pe 's/^version = \"[^\"]+\"/version = \"'$npm_package_version'\"/' Cargo.toml && cargo check && git add Cargo.*"
  }
}
